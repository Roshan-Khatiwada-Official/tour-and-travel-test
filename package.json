{
  "name": "civil-alliance-combined-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Deployment-only entry point that runs the customer and admin backends as one process, so they share a real filesystem for backend/data/*.json. The four apps (frontend, backend, admin-frontend, admin-backend) remain independent for local dev — this file only composes their existing route modules for hosting.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2"
  }
}
